Simple way of achieving chams in older Minecraft versions. By hooking `glScalef` via MinHook, we can identify when entity/player models are being drawn and apply custom OpenGL rendering states (such as disabling depth testing, disabling lighting, or setting custom colors) before passing execution to the original function. Tested on 1.8.9
